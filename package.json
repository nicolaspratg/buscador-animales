{
  "name": "buscador-animales",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm -w server run dev\" \"npm -w client run dev\"",
    "build": "npm -w server run build && npm -w client run build",
    "start": "npm -w server run start",
    "typecheck": "npm -w server run typecheck && npm -w client run typecheck",
    "check": "npm run typecheck && npm test",
    "test": "npm -w server test && npm -w client test"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
